{
  "name": "claude-code-bridge",
  "version": "1.0.0",
  "description": "WebSocket bridge server for Claude Code CLI remote monitoring",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "tunnel": "cloudflared tunnel --url ws://localhost:8765"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
